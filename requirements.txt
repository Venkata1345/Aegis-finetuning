# Aegis — local eval / data prep / baselines / CLI
# (training-only deps live in requirements-train.txt and are Colab-only)

# --- Core ML (CPU-friendly; used for loading the trained adapter for eval) ---
torch>=2.1
transformers>=4.44
peft>=0.11
accelerate>=0.30
datasets>=2.20
huggingface_hub>=0.24

# --- Schema + config ---
pydantic>=2.7
python-dotenv>=1.0

# --- Baselines ---
openai>=1.40
google-generativeai>=0.7
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7  # Presidio NER backbone; en_core_web_lg downloaded by `make setup`

# --- Experiment tracking (also imported during eval to log baseline runs) ---
wandb>=0.17

# --- CLI ---
typer>=0.12
rich>=13.7

# --- Eval / stats ---
numpy>=1.26
scipy>=1.13  # Pearson correlation for calibration check

# --- Held-out eval set construction ---
wikipedia>=1.4  # Wikipedia bio carriers
Faker>=24.0     # Synthetic PII injection

# --- Dev ---
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.5
